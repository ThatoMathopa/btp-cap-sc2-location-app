# Copy to .env — never commit this file.
NODE_ENV=development

# Only needed in production (destinations are used instead in BTP)
SC2_BASE_URL=https://my1001219.de1.test.crm.cloud.sap
SC2_AUTH_HEADER=Bearer <oauth-access-token>

PORT=4004
